# Examples

这里放 `context-bridge` 的最小示例。

## 内容

- `task-handoff.example.json`：任务交接示例
- `task-feedback.example.json`：执行回执示例
- `2026-04-19-example-note.md`：与示例回执配套的说明文档

## 使用方式

- 这些示例只用于理解字段和结构
- 真正项目中请写到对应项目目录下的 `handoffs/` 和 `feedback/`
